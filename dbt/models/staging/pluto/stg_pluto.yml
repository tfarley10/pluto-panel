version: 2

models:
  - name: stg_lot_year
    columns:
      - name: bbl_year_hash_id
        tests:
          - unique
          - not_null
# TODO: USE NEW MAPPING FOR TESTS
      - name: land_use_category
      - name: owner_type_category
              


